```bash
MSF:

> db_status
> workspace -h
> workspace (To see all the workspaces)
> hosts (To have a look about the target)
> workspace -a New (Add a new workspace)
> workspace other_workspace (To switch workspace)
> workspace -d deleted_workspace

```